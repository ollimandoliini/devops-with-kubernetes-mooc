apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: image-pvc
  namespace: project
spec:
  resources:
    requests:
      storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
